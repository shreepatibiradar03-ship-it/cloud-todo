import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.shreepati.calculator',
  appName: 'calculator app',
  webDir: 'build'
};

export default config;
